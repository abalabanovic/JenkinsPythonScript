# JenkinsPythonScript
Python script that interacts with Jenkins API in order to search pipelines on needed specification, in this case it is searching if the pipeline was used in last 2 years and if it is using docker in docker approaches 
